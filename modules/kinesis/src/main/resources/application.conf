{
  "input": ${snowplow.defaults.sources.kinesis}
  "input": {
    "appName": "snowplow-snowflake-loader"
  }
  "output": {
    "bad": ${snowplow.defaults.sinks.kinesis}
    "badRowMaxSize" : ${snowplow.defaults.sinks.kinesis.maxRecordSize}
  }
}
